name: Build

on:
  push:
    branches:
      - "**"

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.final-version.outputs.final_version }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      # Get the previous tag
      - name: Get previous tag
        id: prev
        run: |
          TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "")
          echo "Previous tag: $TAG"
          echo "previous_tag=$TAG" >> $GITHUB_OUTPUT

      # Get the next version (if not SNAPSHOT)
      - name: Get next version
        if: ${{ !endsWith(steps.prev.outputs.previous_tag, '-SNAPSHOT') }}
        id: version
        uses: thenativeweb/get-next-version@main

      # Determine final version
      - name: Determine final version
        id: final-version
        run: |
          PREV="${{ steps.prev.outputs.previous_tag }}"
          NEXT="${{ steps.version.outputs.version }}"
          
          # Remove -SNAPSHOT suffix from previous tag for comparison
          BASE_PREV="${PREV%-SNAPSHOT}"
          
          SKIP_RELEASE="false"
          
          # If NEXT is empty or same as BASE_PREV → bump patch
          if [[ -z "$NEXT" ]] || [[ "$NEXT" == "$BASE_PREV" ]]; then
              IFS='.' read -r MAJOR MINOR PATCH <<< "$BASE_PREV"
              PATCH=$((PATCH + 1))
              NEXT="$MAJOR.$MINOR.$PATCH"
              echo "No version bump detected → bumped patch: $NEXT"
              SKIP_RELEASE="true"
          else
              echo "Next version differs → using NEXT: $NEXT"
          fi
          
          # Always append -SNAPSHOT for final version
          FINAL_VERSION="${NEXT}-SNAPSHOT"
          echo "Final version: $FINAL_VERSION"
          
          echo "final_version=$FINAL_VERSION" >> $GITHUB_OUTPUT
          echo "skip_release=$SKIP_RELEASE" >> $GITHUB_OUTPUT

      # Dependencies
      - name: Create libs directory
        run: mkdir -p libs

      - name: Download Multiverse-Core
        run: |
          curl -L -o libs/Multiverse-Core-4.3.16.jar "https://github.com/Multiverse/Multiverse-Core/releases/download/4.3.16/multiverse-core-4.3.16.jar"

      - name: Download Multiverse-NetherPortals
        run: |
          curl -L -o libs/Multiverse-NetherPortals-4.2.3.jar "https://github.com/Multiverse/Multiverse-NetherPortals/releases/download/4.2.3/multiverse-netherportals-4.2.3.jar"

      # Add dependencies to Maven (.m2) repo
      - name: Install Multiverse-Core to local Maven repo
        run: |
          mvn install:install-file \
            -Dfile=libs/Multiverse-Core-4.3.16.jar \
            -DgroupId=com.github.Multiverse \
            -DartifactId=Multiverse-Core \
            -Dversion=4.3.16 \
            -Dpackaging=jar

      - name: Install Multiverse-NetherPortals to local Maven repo
        run: |
          mvn install:install-file \
            -Dfile=libs/Multiverse-NetherPortals-4.2.3.jar \
            -DgroupId=com.github.Multiverse \
            -DartifactId=Multiverse-NetherPortals \
            -Dversion=4.2.3 \
            -Dpackaging=jar

      # Java & Maven setup
      - name: Set up JDK 11
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "11"

      # - name: Create config.yml
      #   run: |
      #     mkdir -p src/main/resources
      #     printf '%s\n' "${{ vars.SRP_CONFIG_YML }}" > src/main/resources/config.yml

      - name: Build with Maven
        run: |
          mvn -B versions:set -DnewVersion=${{ steps.final-version.outputs.final_version }} -DgenerateBackupPoms=false
          mvn -B clean package

      # Upload resulting JAR for downstream workflows
      - name: Upload built JAR
        if: github.ref == 'refs/heads/n-player-coop' # Only upload if on main-branch
        uses: actions/upload-artifact@v4
        with:
          name: srp-jar
          path: target/*.jar
